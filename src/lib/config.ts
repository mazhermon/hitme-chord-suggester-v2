/**
 * Small site-wide config.
 *
 * FEEDBACK_FORM_URL — paste your free feedback form link here (e.g. a Tally or
 * Google Form). While it's empty, the beta banner shows the note only; once set,
 * a "Want cloud save? Tell us" link appears. No backend or cost involved.
 */
export const FEEDBACK_FORM_URL = ''
