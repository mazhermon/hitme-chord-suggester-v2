// Placeholder home page — the real editor ("Hit me") screen is built in Phase 4.
export default function Page() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1 style={{ fontSize: '3rem', margin: 0 }}>Hit me</h1>
      <p>songwriting tool — rebuild in progress</p>
    </main>
  )
}
